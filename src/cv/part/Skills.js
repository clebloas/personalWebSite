import React, {Component} from 'react';

/*
class Skills extends Component {
    render() {
        return (
            <div id="skills">
                {this.props.skills.map((skill, index) => <div key={index}>
                    <Card>
                    <CardTitle title={skill.categoryName}/>
                    <CardText>{skill.skills}</CardText>
                    </Card>
                </div>)}
            </div>
        );
    }
}
export default Skills;
*/