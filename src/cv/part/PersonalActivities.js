import React, {Component} from 'react';
/*import {Card, CardTitle, CardText} from 'material-ui/Card';

class PersonalActivities extends Component {
    render() {
        return (
            <div id="personnalActivities">
                {this
                    .props
                    .personnalActivities
                    .map((activity, index) => <Card key={index}>
                        <CardTitle title={activity.title}/>
                        <CardText>
                            <div className="activityDescription">{activity.description}</div>
                            <div className="url">{activity.url}</div>
                        </CardText>
                    </Card>)}
            </div>
        );
    }
}
export default PersonalActivities;
*/