import React, {Component} from 'react';

/*class Title extends Component {
    render() {
        return (
            <Card >
                <CardTitle title={this.props.title} subtitle={this.props.title2}/>
            </Card>
        );
    }
}
export default Title;*/